---
import Layout from '@layouts/Layout.astro';
import { DEFAULT_LANGUAGE_DIR, DEFAULT_LANGUAGE_ID, UI_DICTIONARY } from '@lib/languageUtils';
---

<Layout
    lang={DEFAULT_LANGUAGE_ID}
    dir={DEFAULT_LANGUAGE_DIR}
    title={UI_DICTIONARY.website[DEFAULT_LANGUAGE_ID]}
    mode={false}
/>