---
import Layout from '@layouts/Layout.astro';
import { DEFAULT_LANGUAGE_ID } from '@lib/languageUtils';
import { UI_DICTIONARY } from '@lib/uiDictionary';
---

<Layout lang={DEFAULT_LANGUAGE_ID} title={undefined}>
    <h2 class='sr-only'>
        Welcome to {UI_DICTIONARY.websiteTitle[DEFAULT_LANGUAGE_ID]}
    </h2>
    <p class='sr-only'>
        This is the homepage of {UI_DICTIONARY.websiteTitle[DEFAULT_LANGUAGE_ID]}. Use the navigation or featured content to explore.
    </p>
</Layout>