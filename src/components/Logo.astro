---
import { fetchSvg } from '@lib/imageUtils';

type Props = {
    source: string | undefined;
};

const {
    source,
} = Astro.props;

const svgContent = fetchSvg(source);
---

{svgContent ? (
  <Fragment set:html={svgContent} />
) : (
  <svg>
    <!-- TODO -->
  </svg>
)}